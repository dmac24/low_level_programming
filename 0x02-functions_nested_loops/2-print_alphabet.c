#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Read the last digit
 *
 * Return: Always 0.
 */

int main(void)
{
char c = 'a';
while (c < '{')
{
putchar(c);
c++;
}
putchar('\n');

return (0);
}
